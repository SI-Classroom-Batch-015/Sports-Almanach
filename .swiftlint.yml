disabled_rules:
  - force_cast
  - force_unwrapping
  - trailing_whitespace
  - vertical_whitespace
  - multiple_closures_with_trailing_closure
  - large_tuple
  
line_length: 80
  wrap_arguments: after_first
  
type_name:
  min_length: 2
  max_length: 40
  allowed_symbols: ["_"]
  
